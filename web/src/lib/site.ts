/**
 * Site identity. The name is a working title: to rename the site, change it here
 * and in the <title> of index.html (a test keeps the two in sync).
 */
export const SITE_NAME = 'Mental Health Explorer'
